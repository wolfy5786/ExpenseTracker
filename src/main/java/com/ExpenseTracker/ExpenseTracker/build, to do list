Add validation in controller layer
vallidation annotations in DTO

Add exception handling
should we do multi user then we would need user auth! and a user table
if multi user is SQL lite the right choice?